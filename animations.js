
let animations = {
    leaves : function(intensity, duration) {
        // array with images
        
        // while function

        // get random position
        // create elements at the top
        // add css styles to elements
        // move each element down

    }
}